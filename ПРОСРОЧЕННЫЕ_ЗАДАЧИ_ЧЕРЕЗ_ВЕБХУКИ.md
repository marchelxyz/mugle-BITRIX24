# Получение просроченных задач через вебхуки Bitrix24

## Обзор

Система получает информацию о просроченных задачах через **исходящие вебхуки** от Bitrix24. При каждом обновлении задачи (`ONTASKUPDATE`) система автоматически проверяет, просрочена ли задача, и отправляет уведомления.

## Как это работает

### 1. Событие ONTASKUPDATE

Bitrix24 отправляет вебхук `ONTASKUPDATE` при любом изменении задачи:
- Изменение дедлайна
- Изменение статуса
- Изменение исполнителя
- Изменение названия
- Любые другие изменения

### 2. Автоматическая проверка просроченности

При получении события `ONTASKUPDATE` система:

1. **Получает полную информацию о задаче** через REST API Bitrix24
2. **Проверяет дедлайн задачи** - сравнивает с текущей датой и временем
3. **Определяет изменения** - сравнивает текущее состояние с предыдущим из БД
4. **Отправляет уведомления**:
   - Если задача просрочена → отправляет уведомление о просрочке
   - Если есть изменения → отправляет уведомление об изменениях (с предупреждением, если просрочена)

### 3. Логика уведомлений

Система использует два типа уведомлений:

#### Уведомление о просрочке (`overdue`)
- Отправляется **один раз** при обнаружении просроченной задачи
- Ключ уведомления: `{task_id}_overdue_`
- Сообщение: `⚠️ задача «Название»: дедлайн просрочен`

#### Уведомление об обновлении (`task_updated`)
- Отправляется при каждом изменении задачи
- Ключ уведомления: `{task_id}_task_updated_{event}`
- Сообщение зависит от типа изменений

**Важно:** Если задача просрочена и изменилась, система отправит:
1. Уведомление о просрочке (если еще не отправлялось)
2. Уведомление об изменениях с предупреждением ⚠️

## Настройка

### 1. Настройка исходящего вебхука в Bitrix24

1. Откройте Bitrix24 → **Настройки** → **Приложения** → **Исходящий вебхук**
2. Создайте новый обработчик или используйте существующий
3. Настройте обработчик:
   - **Название:** Уведомления о задачах
   - **Событие:** `ONTASKUPDATE`
   - **URL обработчика:** `https://your-domain.com/api/bitrix/webhook`
   - **Метод:** POST
   - **Права:** `tasks` (задачи)
   - **Активен:** Да

4. Сохраните токен вебхука в переменную окружения:
   ```env
   BITRIX24_OUTGOING_WEBHOOK_TOKEN=your-webhook-token
   ```

### 2. Переменные окружения

```env
# Обязательно: URL вашего сервера
WEBHOOK_URL=https://your-domain.com

# Обязательно: Токен исходящего вебхука
BITRIX24_OUTGOING_WEBHOOK_TOKEN=your-webhook-token

# Обязательно: ID Telegram группы для уведомлений
TELEGRAM_SUPERGROUP_ID=-1001234567890

# Опционально: Включить/выключить уведомления о просроченных задачах
ENABLE_OVERDUE_NOTIFICATIONS=true
```

## Примеры работы

### Пример 1: Задача становится просроченной

**Сценарий:**
1. Задача с дедлайном `2024-01-15 18:00:00`
2. Текущая дата: `2024-01-16 10:00:00`
3. Задача обновляется (например, изменяется статус)

**Что происходит:**
1. Bitrix24 отправляет `ONTASKUPDATE`
2. Система получает событие и проверяет дедлайн
3. Обнаруживает, что дедлайн просрочен
4. Отправляет уведомление: `⚠️ задача «Название»: дедлайн просрочен`

### Пример 2: Просроченная задача изменяется

**Сценарий:**
1. Задача уже просрочена
2. Изменяется статус задачи

**Что происходит:**
1. Bitrix24 отправляет `ONTASKUPDATE`
2. Система проверяет просроченность (задача просрочена)
3. Система определяет изменения (статус изменен)
4. Отправляет два уведомления:
   - `⚠️ задача «Название»: дедлайн просрочен` (если еще не отправлялось)
   - `⚠️ задача «Название»: статус изменен на "В работе"` (об изменениях)

### Пример 3: Дедлайн изменяется на просроченный

**Сценарий:**
1. Задача с дедлайном `2024-01-20 18:00:00`
2. Дедлайн изменяется на `2024-01-15 18:00:00` (прошлая дата)

**Что происходит:**
1. Bitrix24 отправляет `ONTASKUPDATE` с `FIELDS_BEFORE` и `FIELDS_AFTER`
2. Система определяет изменение дедлайна
3. Система проверяет новый дедлайн - он просрочен
4. Отправляет уведомление: `⚠️ задача «Название»: дедлайн просрочен`

## Преимущества подхода через вебхуки

✅ **Реальное время** - уведомления приходят сразу при изменении задачи  
✅ **Надежность** - Bitrix24 гарантирует доставку событий  
✅ **Эффективность** - не нужно периодически опрашивать API  
✅ **Точность** - получаем точные данные о изменениях из вебхука  
✅ **Автоматизация** - работает без дополнительной настройки  

## Обработка граничных случаев

### Задача без дедлайна

Если у задачи нет дедлайна, она не может быть просрочена. Система пропускает проверку.

### Задача уже завершена

Система проверяет просроченность для всех задач, включая завершенные. Но обычно завершенные задачи не считаются просроченными (можно настроить через `exclude_status`).

### Множественные обновления

Если задача обновляется несколько раз подряд:
- Уведомление о просрочке отправляется **один раз**
- Уведомления об изменениях отправляются при каждом обновлении

## Отключение периодического опроса

Если вы используете вебхуки для получения просроченных задач, можно отключить периодический опрос:

```env
# Отключить периодическую проверку просроченных задач
ENABLE_OVERDUE_NOTIFICATIONS=false
```

Или оставить включенным как резервный механизм (на случай, если вебхук не сработал).

## Логирование

Система логирует следующие события:

- `📋 Получено событие задачи: ONTASKUPDATE` - получен вебхук
- `🔍 Обработка события задачи {id}` - начало обработки
- `⚠️ Задача {id} просрочена, отправляем уведомление о просрочке` - обнаружена просрочка
- `✅ Отправлено уведомление о событии {event} для задачи {id}` - уведомление отправлено

## Отладка

### Проверка получения вебхуков

Используйте команду `/webhooks ONTASKUPDATE` в боте для просмотра последних событий обновления задач.

### Проверка конкретного вебхука

Используйте команду `/webhook <id>` для просмотра детальной информации о вебхуке.

### Проверка логов

В логах приложения должны появляться сообщения:
```
📋 Получено событие задачи: ONTASKUPDATE
🔍 Обработка события задачи 123
⚠️ Задача 123 просрочена, отправляем уведомление о просрочке
✅ Отправлено уведомление о событии ONTASKUPDATE для задачи 123
```

## Сравнение с периодическим опросом

### Вебхуки (рекомендуется) ✅

- **Преимущества:**
  - Реальное время
  - Меньше нагрузка на API
  - Точные данные об изменениях
  - Автоматическая работа

- **Недостатки:**
  - Требует настройки вебхука
  - Зависит от доступности сервера

### Периодический опрос

- **Преимущества:**
  - Не требует настройки вебхука
  - Работает даже если вебхук недоступен

- **Недостатки:**
  - Задержка до следующей проверки
  - Больше нагрузка на API
  - Может пропустить кратковременные изменения

## Рекомендации

1. **Используйте вебхуки** как основной способ получения просроченных задач
2. **Оставьте периодический опрос** как резервный механизм (с большим интервалом, например, раз в день)
3. **Настройте мониторинг** вебхуков для отслеживания проблем
4. **Проверяйте логи** регулярно для выявления проблем

## Заключение

Система автоматически получает информацию о просроченных задачах через вебхуки Bitrix24. При каждом обновлении задачи система проверяет просроченность и отправляет уведомления в реальном времени.
