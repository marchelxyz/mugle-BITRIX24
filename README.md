# Telegram бот для Битрикс24

Telegram бот-мини приложение для создания задач в Битрикс24 через @ упоминания.

## Возможности

- ✅ Создание задач в Битрикс24 через Telegram бота
- ✅ Пошаговый диалог для уточнения деталей задачи
- ✅ Назначение нескольких ответственных через запятую
- ✅ Установка срока выполнения задачи
- ✅ Добавление описания и файлов к задаче
- ✅ Автоматическое определение создателя задачи по Telegram аккаунту
- ✅ **НОВОЕ:** Создание задачи через ответ на сообщение с упоминанием бота
- ✅ **НОВОЕ:** Telegram Mini App для удобного создания задач (открывается на половину экрана)
- ✅ **НОВОЕ:** Автоматическое заполнение постановщика и исполнителя при ответе на сообщение
- ✅ **НОВОЕ:** Сохранение Telegram ID пользователей в Bitrix24 для автоматического определения
- ✅ **НОВОЕ:** Автоматический выбор подразделения на основе темы (thread) в Telegram супергруппе

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd bitrix-24-bot-muglerest-fr
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения в файле `.env`:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
BITRIX24_DOMAIN=your-domain.bitrix24.ru
BITRIX24_WEBHOOK_TOKEN=your_webhook_token_here
BITRIX24_USER_ID=your_user_id_here

# Опционально: маппинг тем Telegram -> подразделения Bitrix24
THREAD_DEPARTMENT_MAPPING={"123": 5, "456": 10}
```

## Настройка

### 1. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в переменную `TELEGRAM_BOT_TOKEN`

#### Настройка Main Mini App (для работы в публичных группах)

Для того, чтобы ссылки открывались как мини-апп из публичных групп, нужно настроить Main Mini App:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Выберите вашего бота (команда `/mybots` → выберите бота)
3. Выберите **Bot Settings** → **Main Mini App**
4. Укажите URL вашего Mini App (например: `https://your-domain.com/miniapp`)
5. После настройки ссылки формата `https://t.me/botusername?startapp=token` будут открывать Mini App как мини-апп

**Примечание:** Это необходимо для работы Direct Link Mini Apps в публичных группах. Без этой настройки ссылки будут открываться в браузере вместо мини-аппа.

### 2. Настройка Битрикс24

1. Войдите в ваш портал Битрикс24
2. Перейдите в **Настройки** → **Приложения** → **Входящий вебхук**
3. Создайте новый входящий вебхук с правами:
   - `task` (задачи) - для создания задач
   - `user` (пользователи) - для работы с пользователями
   - `user.userfield` (пользовательские поля) - **ВАЖНО:** для автоматического создания поля "Telegram" в профилях пользователей
   - `department` (подразделения) - опционально, для работы с подразделениями
4. Скопируйте URL вебхука и извлеките из него:
   - Домен (например, `your-domain.bitrix24.ru`)
   - Токен вебхука (последняя часть URL после `/rest/`)
5. Заполните переменные `BITRIX24_DOMAIN` и `BITRIX24_WEBHOOK_TOKEN`

**Примечание:** Если вы не включите права `user.userfield`, бот не сможет автоматически создать поле "Telegram" в профилях пользователей. В этом случае вам нужно будет создать поле вручную:
- Перейдите в **Настройки** → **Пользователи** → **Пользовательские поля**
- Создайте новое поле с кодом `UF_TELEGRAM` (или другим, если настроено через `BITRIX24_TELEGRAM_FIELD_NAME`)
- Тип поля: **Строка**

### 3. Получение User ID в Битрикс24

1. Войдите в Битрикс24
2. Перейдите в **Настройки** → **Пользователи**
3. Откройте профиль нужного пользователя
4. В URL будет ID пользователя (например, `/company/personal/user/123/`)
5. Используйте этот ID для переменной `BITRIX24_USER_ID`

### 4. Настройка автоматического выбора подразделения по теме (опционально)

Если ваша Telegram супергруппа разбита на темы (threads), соответствующие подразделениям в Bitrix24, вы можете настроить автоматический выбор отдела:

1. **Получите ID тем в Telegram:**
   - Откройте тему в супергруппе
   - ID темы можно получить через API Telegram или из URL (если доступен)
   - Обычно это числовое значение, например: `123`, `456`

2. **Получите ID подразделений в Bitrix24:**
   - Войдите в Bitrix24
   - Перейдите в **Настройки** → **Структура компании** → **Подразделения**
   - Откройте нужное подразделение
   - ID подразделения можно найти в URL или через API

3. **Настройте маппинг:**
   - Добавьте переменную `THREAD_DEPARTMENT_MAPPING` в `.env` файл или в переменные окружения Railway
   - Формат: JSON объект `{"thread_id": department_id}`
   - Пример: `THREAD_DEPARTMENT_MAPPING={"123": 5, "456": 10}`
   - Где `123` и `456` - ID тем в Telegram, `5` и `10` - ID подразделений в Bitrix24

4. **Как это работает:**
   - Когда задача создается из определенной темы в Telegram супергруппе, бот автоматически определяет соответствующее подразделение
   - Подразделение автоматически выбирается в форме создания задачи
   - Пользователь может изменить выбранное подразделение вручную, если нужно

**Примечание:** 
- Если маппинг не настроен, поле подразделения будет пустым, и пользователь сможет выбрать его вручную из списка всех подразделений Bitrix24.
- По умолчанию используется поле `GROUP_ID` для связи задачи с подразделением (если подразделения настроены как группы проектов в Bitrix24). Если в вашем Bitrix24 используется другое поле для подразделений в задачах, измените поле `GROUP_ID` на нужное в файле `bitrix24_client.py` в методе `create_task`.

## Использование

### Запуск бота

```bash
python bot.py
```

### Автоматическое создание поля "Telegram" в профилях пользователей

**Бот автоматически создает поле "Telegram" в Bitrix24 при первом запуске**, если вебхук имеет необходимые права.

**Необходимые права для вебхука:**
- `user.userfield.add` - для создания пользовательского поля
- `user.userfield.get` - для проверки существования поля
- `user.update` - для сохранения Telegram ID в профиль пользователя

**Как включить права:**
1. Перейдите в **Настройки** → **Разработчикам** → **Входящий вебхук**
2. Выберите ваш вебхук
3. В разделе "Права доступа" включите:
   - ✅ `user.userfield` (или отдельно `user.userfield.add` и `user.userfield.get`)
   - ✅ `user` (для обновления профилей пользователей)

**Если права не включены:**
- Бот попытается создать поле автоматически, но выдаст предупреждение
- Вам нужно будет создать поле вручную:
  1. Перейдите в **Настройки** → **Пользователи** → **Пользовательские поля**
  2. Создайте новое поле с кодом `UF_TELEGRAM` (или другим, если настроено через `BITRIX24_TELEGRAM_FIELD_NAME`)
  3. Тип поля: **Строка**
  4. Название поля: **Telegram** (или любое другое)

**Важно:** Поле создается один раз и становится доступным для всех пользователей в их профилях. После создания поле будет автоматически использоваться для сохранения Telegram ID при команде `/link`.

### Связывание пользователей

Перед использованием необходимо связать ваш Telegram аккаунт с ID пользователя Битрикс24:

```
/link bitrix_user_id
```

Пример:
```
/link 123
```

**Что происходит при команде `/link`:**
1. Бот проверяет существование поля "Telegram" в Bitrix24 (или создает его, если есть права)
2. Бот сохраняет ваш Telegram ID в поле "Telegram" профиля пользователя Bitrix24
3. После этого бот автоматически определяет ваш аккаунт по Telegram ID

Для поиска пользователей по имени можно также связать Telegram username:

```
/link_username @username bitrix_user_id
```

Пример:
```
/link_username @ivanov 123
```

### Просмотр подразделений

Для просмотра всех подразделений из Bitrix24 используйте команду:
```
/departments
```

Эта команда выведет список всех подразделений с их ID и названиями. Это полезно для настройки маппинга `THREAD_DEPARTMENT_MAPPING`.

**Примечание:** При запуске бота все подразделения автоматически загружаются и выводятся в логи сервера с их ID и названиями.

### Создание задачи

1. Упомяните бота в сообщении с текстом задачи:
   ```
   @bitmugle, Зум по встрече с партнерами
   ```

2. Бот задаст вам вопросы:
   - **На кого задача?** (можно указать несколько человек через запятую)
     - Пример: `Иван Иванов, Петр Петров`
   
   - **Какой срок?** (формат: дд.мм.гг чч:мм)
     - Пример: `25.12.24 15:30`
   
   - **Введите описание** (можно пропустить, отправив `-` или `пропустить`)
   
   - **Прикрепите файлы** (можно пропустить, отправив `-`)

3. После ответа на все вопросы бот создаст задачу в Битрикс24.

### Отмена создания задачи

В любой момент можно отменить создание задачи командой:
```
/cancel
```

### Создание задачи через ответ на сообщение (Mini App)

**Новая функция!** Теперь можно создавать задачи еще проще:

1. Ответьте на любое сообщение в чате и упомяните бота через @:
   ```
   @bot
   ```
   или просто напишите имя бота в ответе

2. Бот отправит сообщение с предложением создать задачу и кнопкой "📋 Создать задачу"

3. Нажмите на кнопку - откроется Telegram Mini App (веб-приложение на половину экрана)

4. В форме автоматически будут заполнены:
   - **Постановщик**: вы (тот, кто ответил и упомянул бота)
   - **Исполнитель**: автор сообщения, на которое вы ответили
   - **Подразделение**: автоматически определяется на основе темы (thread), если настроен маппинг `THREAD_DEPARTMENT_MAPPING`
   - **Описание**: текст сообщения, на которое вы ответили (автоматически добавляется в описание задачи)

5. Заполните остальные поля:
   - Название задачи (обязательно)
   - Подразделение (опционально, можно выбрать из списка или изменить автоматически определенное)
   - Срок выполнения (опционально)
   - Дополнительное описание (опционально)

6. При необходимости можно изменить постановщика, исполнителя или подразделение, нажав кнопку "Изменить" или выбрав из списка

7. Нажмите "Создать задачу" - задача будет создана в Bitrix24!

**Преимущества:**
- Быстрое создание задач прямо из чата
- Автоматическое определение участников
- Удобный интерфейс Mini App
- Текст сообщения автоматически добавляется в описание задачи

## Деплой на Railway

Проект готов к деплою на Railway без использования командной строки и .env файлов.

### Шаги для деплоя:

1. **Подключите репозиторий к Railway:**
   - Зайдите на [Railway](https://railway.app)
   - Создайте новый проект
   - Подключите ваш GitHub репозиторий

2. **Настройте переменные окружения в Railway:**
   - В интерфейсе Railway перейдите в раздел **Variables**
   - Добавьте следующие переменные:
     - `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
     - `BITRIX24_DOMAIN` - домен Битрикс24 (например, `your-domain.bitrix24.ru`)
     - `BITRIX24_WEBHOOK_TOKEN` - токен вебхука Битрикс24
     - `BITRIX24_USER_ID` - ваш ID пользователя в Битрикс24 (опционально, для дефолтного создателя)
     - `WEBHOOK_URL` - URL вашего приложения на Railway (Railway автоматически предоставляет переменную `RAILWAY_PUBLIC_DOMAIN`, но можно использовать полный URL)
     - `THREAD_DEPARTMENT_MAPPING` - маппинг тем Telegram -> подразделения Bitrix24 (опционально, формат: `{"123": 5, "456": 10}`)

3. **Настройте webhook для Telegram:**
   - После деплоя Railway предоставит публичный URL вашего приложения
   - Добавьте переменную `WEBHOOK_URL` в Railway со значением `https://${{RAILWAY_PUBLIC_DOMAIN}}`
     (Railway автоматически подставит ваш домен)
   - Или установите webhook вручную через API Telegram:
     ```
     https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://<YOUR_RAILWAY_DOMAIN>/<YOUR_BOT_TOKEN>
     ```
   - Бот автоматически настроит webhook при запуске, если установлены `PORT` и `WEBHOOK_URL`

4. **Railway автоматически:**
   - Определит Python из `runtime.txt`
   - Установит зависимости из `requirements.txt`
   - Запустит приложение через `Procfile`

### Примечания:

- Railway автоматически устанавливает переменную `PORT` - бот использует её для webhook
- Если `PORT` установлен, но `WEBHOOK_URL` не найден, бот попытается использовать `RAILWAY_PUBLIC_DOMAIN`
- Если `PORT` не установлен, бот будет работать в режиме polling (для локальной разработки)
- Все переменные окружения настраиваются через интерфейс Railway, файл `.env` не требуется
- После первого деплоя проверьте логи Railway, чтобы убедиться, что webhook установлен корректно

## Структура проекта

```
.
├── bot.py                 # Основной файл Telegram бота
├── bitrix24_client.py     # Клиент для работы с API Битрикс24
├── requirements.txt       # Зависимости Python
├── Procfile              # Конфигурация для Railway
├── runtime.txt          # Версия Python для Railway
├── set_webhook.py        # Скрипт для установки webhook (опционально)
├── .env.example          # Пример файла конфигурации (для локальной разработки)
├── .gitignore            # Игнорируемые файлы
└── README.md             # Документация
```

## API Битрикс24

Бот использует следующие методы API Битрикс24:

- `tasks.task.add` - создание задачи
- `user.get` - получение информации о пользователе
- `user.search` - поиск пользователей

## Разработка

### Добавление новых функций

1. Расширьте класс `Bitrix24Client` в `bitrix24_client.py` для новых методов API
2. Добавьте новые обработчики в `bot.py`
3. Обновите документацию

### Хранение соответствий пользователей

В текущей версии соответствия Telegram username → Bitrix24 User ID хранятся в памяти (`USER_MAPPING`). Для продакшена рекомендуется использовать базу данных (например, SQLite или PostgreSQL).

## Лицензия

MIT
