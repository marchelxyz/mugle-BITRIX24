# Получение информации о просроченных задачах

## Обзор

Система предоставляет надежный метод для получения просроченных задач из Bitrix24. Метод автоматически использует несколько стратегий для обеспечения максимальной надежности.

## Метод `get_overdue_tasks()`

### Описание

Метод `get_overdue_tasks()` в классе `Bitrix24Client` получает все просроченные задачи из Bitrix24.

### Использование

```python
from bitrix24_client import Bitrix24Client

# Инициализация клиента
client = Bitrix24Client(
    domain="your-domain.bitrix24.ru",
    webhook_token="your-webhook-token"
)

# Получение просроченных задач
overdue_tasks = client.get_overdue_tasks()

# Обработка результатов
for task in overdue_tasks:
    print(f"Задача {task['id']}: {task['title']}")
    print(f"  Дедлайн: {task['deadline']}")
    print(f"  Ответственный: {task['responsibleId']}")
    print(f"  Статус: {task['status']}")
```

### Параметры

- `exclude_status` (опционально): Список статусов для исключения из результатов
  - По умолчанию: `[5]` (исключаются завершенные задачи)
  - Пример: `exclude_status=[5, 7]` - исключить завершенные и отклоненные задачи

### Возвращаемое значение

Список словарей с информацией о просроченных задачах. Каждая задача содержит:

- `id` - ID задачи в Bitrix24
- `title` - Название задачи
- `deadline` - Дедлайн задачи (строка в формате даты)
- `status` - Статус задачи
- `responsibleId` - ID ответственного пользователя
- `createdBy` - ID создателя задачи
- `createdDate` - Дата создания
- `changedDate` - Дата последнего изменения

### Примеры использования

#### Пример 1: Получение всех просроченных задач

```python
overdue_tasks = client.get_overdue_tasks()

print(f"Найдено просроченных задач: {len(overdue_tasks)}")
for task in overdue_tasks:
    print(f"- {task['title']} (ID: {task['id']})")
```

#### Пример 2: Исключение нескольких статусов

```python
# Исключить завершенные (5) и отклоненные (7) задачи
overdue_tasks = client.get_overdue_tasks(exclude_status=[5, 7])
```

#### Пример 3: Получение только активных просроченных задач

```python
# Исключить завершенные (5), отложенные (6) и отклоненные (7)
overdue_tasks = client.get_overdue_tasks(exclude_status=[5, 6, 7])
```

## Как это работает

Метод использует двухуровневую стратегию для надежного получения данных:

### Стратегия 1: Фильтрация через API

1. Пробует фильтрацию через Bitrix24 API с оператором `<DEADLINE`
2. Использует разные форматы даты для совместимости:
   - `YYYY-MM-DD HH:MM:SS` (с временем)
   - `YYYY-MM-DD` (только дата)
   - `YYYY-MM-DDT HH:MM:SS` (ISO формат)
3. Дополнительно проверяет результаты в коде для точности

### Стратегия 2: Фильтрация в коде

Если API фильтрация не работает:

1. Получает все незавершенные задачи
2. Фильтрует их в коде, проверяя каждый дедлайн
3. Поддерживает разные форматы дат:
   - ISO формат с временной зоной (`2024-01-15T18:00:00+03:00`)
   - Формат `YYYY-MM-DD HH:MM:SS`
   - Формат `YYYY-MM-DD` (считается как конец дня 23:59:59)

## Вспомогательный метод `_is_task_overdue()`

Метод `_is_task_overdue()` проверяет, просрочена ли конкретная задача:

```python
task = {
    'id': 123,
    'deadline': '2024-01-15 18:00:00',
    'title': 'Пример задачи'
}

is_overdue = client._is_task_overdue(task)
```

### Параметры

- `task`: Словарь с данными задачи
- `current_time` (опционально): Время для сравнения (по умолчанию `datetime.now()`)

### Возвращаемое значение

- `True` - если задача просрочена (дедлайн < текущее время)
- `False` - если задача не просрочена или нет дедлайна

## Интеграция с системой уведомлений

Метод автоматически используется в системе уведомлений (`TaskNotificationService`):

```python
# В task_notifications.py метод check_overdue_tasks() использует:
tasks = self.bitrix_client.get_overdue_tasks(exclude_status=[5])
```

Система уведомлений автоматически:
1. Получает просроченные задачи
2. Проверяет, не отправлялись ли уже уведомления
3. Отправляет уведомления в Telegram группу
4. Сохраняет информацию об отправленных уведомлениях

## Статусы задач в Bitrix24

- `1` - Новая
- `2` - В работе
- `3` - Ожидает выполнения
- `4` - Требует внимания
- `5` - Завершена
- `6` - Отложена
- `7` - Отклонена

## Обработка ошибок

Метод обрабатывает следующие ситуации:

1. **Ошибки API** - автоматически переключается на фильтрацию в коде
2. **Неправильный формат даты** - пропускает задачу с логированием ошибки
3. **Отсутствие дедлайна** - задача не считается просроченной
4. **Проблемы с сетью** - возвращает пустой список с логированием ошибки

## Логирование

Метод логирует следующие события:

- `🔍 Поиск просроченных задач` - начало поиска
- `✅ Найдено X просроченных задач` - успешное получение результатов
- `❌ Ошибка при получении просроченных задач` - ошибка выполнения
- `Ошибка при парсинге дедлайна` - ошибка парсинга конкретной задачи (debug уровень)

## Производительность

- **Стратегия 1 (API фильтрация)**: Быстрее, так как фильтрация происходит на стороне Bitrix24
- **Стратегия 2 (фильтрация в коде)**: Медленнее, но более надежно, так как получает все задачи и фильтрует локально

Рекомендуется использовать стратегию 1, если она работает в вашем случае.

## Пример полного использования

```python
from bitrix24_client import Bitrix24Client
from datetime import datetime

# Инициализация
client = Bitrix24Client(
    domain=os.getenv("BITRIX24_DOMAIN"),
    webhook_token=os.getenv("BITRIX24_WEBHOOK_TOKEN")
)

# Получение просроченных задач
overdue_tasks = client.get_overdue_tasks(exclude_status=[5, 7])

# Обработка
if overdue_tasks:
    print(f"\n⚠️ Найдено {len(overdue_tasks)} просроченных задач:\n")
    
    for task in overdue_tasks:
        task_id = task['id']
        title = task['title']
        deadline = task['deadline']
        responsible_id = task['responsibleId']
        
        # Получение URL задачи
        task_url = client.get_task_url(task_id, responsible_id)
        
        print(f"📋 {title}")
        print(f"   ID: {task_id}")
        print(f"   Дедлайн: {deadline}")
        print(f"   Ответственный: {responsible_id}")
        print(f"   Ссылка: {task_url}\n")
else:
    print("✅ Просроченных задач не найдено")
```

## Заключение

Метод `get_overdue_tasks()` предоставляет надежный способ получения просроченных задач из Bitrix24 с автоматическим переключением между стратегиями для обеспечения максимальной надежности.
