# Проблема с токеном исходящего вебхука Bitrix24

## 🔍 Проблема

В логах появляется ошибка:
```
⚠️ Неверный токен исходящего вебхука. Получен: None...
```

Это означает, что токен не найден в запросе от Bitrix24.

## 📋 Используемые переменные окружения

### 1. `BITRIX24_OUTGOING_WEBHOOK_TOKEN` (обязательно)

**Что это:** Токен исходящего вебхука, который вы получили при создании вебхука в Bitrix24.

**Где установить:**
- **Railway:** Variables → Добавить переменную `BITRIX24_OUTGOING_WEBHOOK_TOKEN` со значением токена
- **Локально:** Добавить в файл `.env`

**Пример значения:**
```
BITRIX24_OUTGOING_WEBHOOK_TOKEN=ssmtodolnza09g2rnm1ucbyzumejtr39
```

**Важно:** Эта переменная должна быть установлена на сервере!

### 2. `WEBHOOK_URL` (опционально, но рекомендуется)

**Что это:** URL вашего приложения для исходящего вебхука.

**Пример:**
```
WEBHOOK_URL=https://web-production-de105.up.railway.app
```

## 🔍 Где Bitrix24 отправляет токен?

Код проверяет токен в следующих местах (в порядке приоритета):

1. **Заголовок `X-Bitrix-Token`** - основной способ
2. **Заголовок `Authorization: Bearer <token>`** - альтернативный способ
3. **Параметр URL `?token=xxx`** - если токен в URL
4. **Тело запроса `data.token`** - если токен в JSON
5. **Тело запроса `data.auth.token`** - если токен вложен в auth

## ❌ Почему токен не найден?

Возможные причины:

1. **Переменная `BITRIX24_OUTGOING_WEBHOOK_TOKEN` не установлена**
   - Проверьте переменные окружения на сервере
   - Убедитесь, что переменная добавлена и приложение перезапущено

2. **Bitrix24 не отправляет токен в запросе**
   - Проверьте настройки вебхука в Bitrix24
   - Убедитесь, что токен указан в настройках вебхука
   - Возможно, токен отправляется в другом формате

3. **Токен отправляется в неожиданном месте**
   - Bitrix24 может отправлять токен в другом заголовке или формате
   - Нужно проверить логи с полными заголовками запроса

## ✅ Решение

### Шаг 1: Проверьте переменную окружения

Убедитесь, что переменная `BITRIX24_OUTGOING_WEBHOOK_TOKEN` установлена:

**Для Railway:**
1. Откройте проект в Railway
2. Перейдите в раздел **Variables**
3. Проверьте, есть ли переменная `BITRIX24_OUTGOING_WEBHOOK_TOKEN`
4. Если нет - добавьте её со значением токена из Bitrix24

**Для проверки через код:**
Добавьте временно в код логирование:
```python
logger.info(f"BITRIX24_OUTGOING_WEBHOOK_TOKEN установлен: {bool(os.getenv('BITRIX24_OUTGOING_WEBHOOK_TOKEN'))}")
```

### Шаг 2: Проверьте настройки вебхука в Bitrix24

1. Войдите в Bitrix24
2. Перейдите в **Настройки** → **Приложения** → **Исходящий вебхук**
3. Найдите ваш вебхук для события `ONUSERUPDATE` или других событий
4. Проверьте, что токен указан в настройках вебхука
5. Убедитесь, что URL вебхука правильный: `https://web-production-de105.up.railway.app/api/bitrix/webhook`

### Шаг 3: Включите отладку заголовков

Чтобы увидеть, какие заголовки отправляет Bitrix24, можно временно добавить логирование всех заголовков запроса.

### Шаг 4: Временное решение (только для отладки!)

Если нужно временно отключить проверку токена для отладки:

**ВНИМАНИЕ:** Это небезопасно! Используйте только для отладки!

В коде `bot.py` найдите строку:
```python
if outgoing_webhook_token:
```

И временно закомментируйте проверку токена (только для отладки!).

## 🔧 Дополнительная отладка

Чтобы увидеть все заголовки запроса от Bitrix24, можно добавить временное логирование:

```python
logger.info(f"Все заголовки запроса: {dict(request.headers)}")
logger.info(f"Параметры URL: {dict(request.query)}")
logger.info(f"Тело запроса (первые 1000 символов): {body_text[:1000]}")
```

Это поможет понять, в каком формате Bitrix24 отправляет токен.

## 📚 Дополнительная информация

- [`НАСТРОЙКА_ТОКЕНА.md`](НАСТРОЙКА_ТОКЕНА.md) - инструкция по настройке токена
- [`BITRIX_OUTGOING_WEBHOOK.md`](BITRIX_OUTGOING_WEBHOOK.md) - подробная документация по вебхуку
