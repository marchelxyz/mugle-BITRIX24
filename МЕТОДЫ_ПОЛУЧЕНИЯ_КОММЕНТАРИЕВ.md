# Методы получения текста комментариев к задачам

## Обзор

Для получения текста комментариев к задачам в Bitrix24 реализована система с **12 пронумерованными методами**, которые пробуются по очереди до первого рабочего.

## Основной метод

### `get_task_comment_text_multiple_methods(task_id, message_id, chat_id=None)`

Автоматически пробует все доступные методы получения текста комментария и возвращает текст первого успешного метода.

**Параметры:**
- `task_id` (int): ID задачи
- `message_id` (int): ID сообщения (MESSAGE_ID из вебхука ONTASKCOMMENTADD)
- `chat_id` (int, опционально): ID чата задачи (будет получен автоматически если не указан)

**Возвращает:**
- `str` или `None`: Текст комментария или None, если не удалось получить

**Пример использования:**
```python
comment_text = bitrix_client.get_task_comment_text_multiple_methods(
    task_id=41117,
    message_id=1741091,
    chat_id=43835  # опционально
)
if comment_text:
    print(f"Текст комментария: {comment_text}")
```

## Пронумерованные методы

### Метод 1: `im.message.get` (chatId, id)
**Формат параметров:** camelCase  
**Параметры:** `{"chatId": chat_id, "id": message_id}`  
**Описание:** Стандартный метод получения сообщения из чата с camelCase параметрами.

### Метод 2: `im.message.get` (CHAT_ID, ID)
**Формат параметров:** UPPERCASE  
**Параметры:** `{"CHAT_ID": chat_id, "ID": message_id}`  
**Описание:** Стандартный метод получения сообщения из чата с UPPERCASE параметрами.

### Метод 3: `im.message.get` (CHAT_ID, id)
**Формат параметров:** Смешанный  
**Параметры:** `{"CHAT_ID": chat_id, "id": message_id}`  
**Описание:** Стандартный метод получения сообщения из чата со смешанным форматом параметров.

### Метод 4: `im.message.get` (только ID)
**Формат параметров:** Только ID  
**Параметры:** `{"ID": message_id}`  
**Описание:** Попытка получить сообщение только по ID без указания чата (может работать в некоторых версиях Bitrix24).

### Метод 5: `im.message.get` (MESSAGE_ID)
**Формат параметров:** MESSAGE_ID вместо ID  
**Параметры:** `{"MESSAGE_ID": message_id}`  
**Описание:** Попытка использовать MESSAGE_ID вместо ID (соответствует формату вебхука).

### Метод 6: `im.message.list` + поиск по ID
**Метод:** `im.message.list` с последующим поиском  
**Параметры:** `{"CHAT_ID": chat_id, "LIMIT": 100}`  
**Описание:** Получение списка сообщений из чата и поиск нужного сообщения по ID в списке.

### Метод 7: `tasks.task.comment.get`
**Метод:** Старый метод получения комментариев  
**Параметры:** `{"taskId": task_id, "commentId": message_id}`  
**Описание:** Использование старого API комментариев задач (может не работать в новых версиях Bitrix24).

### Метод 8: `im.dialog.messages.get`
**Метод:** Получение сообщений диалога  
**Параметры:** `{"DIALOG_ID": chat_id, "LIMIT": 100}`  
**Описание:** Попытка получить сообщения через API диалогов с последующим поиском по ID.

**Подробная документация:** См. [IM_DIALOG_MESSAGES_GET_API.md](IM_DIALOG_MESSAGES_GET_API.md) для полного описания параметров и примеров использования.

### Метод 9: `im.dialog.get` + `im.message.list`
**Методы:** Комбинация методов  
**Описание:** Сначала получаем информацию о диалоге, затем список сообщений и ищем нужное.

### Метод 10: `im.chat.get` + `im.message.list`
**Методы:** Комбинация методов  
**Описание:** Сначала получаем информацию о чате, затем список сообщений и ищем нужное.

### Метод 11: `im.message.get` (chatId, messageId)
**Формат параметров:** camelCase с messageId  
**Параметры:** `{"chatId": chat_id, "messageId": message_id}`  
**Описание:** Вариант с использованием messageId вместо id.

### Метод 12: `im.message.get` (CHAT_ID, MESSAGE_ID)
**Формат параметров:** UPPERCASE с MESSAGE_ID  
**Параметры:** `{"CHAT_ID": chat_id, "MESSAGE_ID": message_id}`  
**Описание:** Вариант с использованием MESSAGE_ID в UPPERCASE формате.

## Вспомогательные методы

Каждый пронумерованный метод реализован как отдельная функция:
- `_try_get_message_method1()` - `_try_get_message_method12()`

Эти методы вызываются автоматически основным методом `get_task_comment_text_multiple_methods()`.

## Логирование

Все методы логируют свои попытки:
- ✅ Успешное получение текста
- ❌ Ошибки при попытке
- ⚠️ Предупреждения о проблемах

Пример логов:
```
🔍 Попытка: Метод 1: im.message.get (chatId, id)
✅ Успешно: Метод 1: im.message.get (chatId, id) - получен текст комментария
```

## Автоматическое получение chatId

Если `chat_id` не указан, метод автоматически получает его из задачи:
```python
task_info = self.get_task_by_id(task_id)
chat_id = task_info.get('chatId') or task_info.get('chat_id')
```

## Интеграция с обработчиком комментариев

Метод автоматически используется в `handle_task_comment_event()` в `task_notifications.py`:

1. Сначала пробуется старый метод `get_task_chat_message()` для совместимости
2. Если не сработал, используется `get_task_comment_text_multiple_methods()`
3. Текст комментария добавляется в уведомление

## Требования к правам вебхука

Для работы методов требуются права на следующие методы Bitrix24 API:

### Обязательные:
- `tasks.task.get` - для получения информации о задаче и chatId
- `im.message.get` - для получения сообщений из чатов
- `im.message.list` - для получения списка сообщений (fallback)

### Опциональные (для дополнительных методов):
- `im.chat.get` - для получения информации о чате
- `im.dialog.get` - для получения информации о диалоге
- `im.dialog.messages.get` - для получения сообщений диалога (см. [IM_DIALOG_MESSAGES_GET_API.md](IM_DIALOG_MESSAGES_GET_API.md))
- `tasks.task.comment.get` - для старого метода (может не работать)

**Настройка прав:**
1. Bitrix24 → Настройки → Разработчикам → Входящий вебхук
2. Выберите ваш вебхук → Права доступа
3. Найдите раздел **"Мессенджер (im)"** и включите права на модуль или конкретные методы

## Рекомендации

1. **Для продакшена:** Используйте основной метод `get_task_comment_text_multiple_methods()` - он автоматически найдет рабочий метод.

2. **Для отладки:** Проверьте логи, чтобы увидеть, какой метод сработал. Это поможет определить оптимальный метод для вашей версии Bitrix24.

3. **Оптимизация:** Если вы знаете, какой метод работает в вашей версии Bitrix24, можно использовать его напрямую через соответствующий `_try_get_message_methodN()`.

4. **Права вебхука:** Убедитесь, что у вебхука есть права на модуль `im` или конкретные методы `im.message.get` и `im.message.list`.

## Примеры использования

### Базовое использование
```python
from bitrix24_client import Bitrix24Client

client = Bitrix24Client(domain="your-domain.bitrix24.ru", webhook_token="your-token")

# Получение текста комментария
comment_text = client.get_task_comment_text_multiple_methods(
    task_id=41117,
    message_id=1741091
)

if comment_text:
    print(f"Текст комментария: {comment_text}")
else:
    print("Не удалось получить текст комментария")
```

### Использование с указанием chatId
```python
comment_text = client.get_task_comment_text_multiple_methods(
    task_id=41117,
    message_id=1741091,
    chat_id=43835
)
```

### Использование в обработчике вебхука
```python
# В handle_task_comment_event автоматически используется:
comment_text = api_client.get_task_comment_text_multiple_methods(
    task_id=task_id_int,
    message_id=message_id_int,
    chat_id=chat_id
)
```

## Диагностика проблем

### Проблема: Все методы возвращают None

**Возможные причины:**
1. Нет прав вебхука на методы `im.message.get` или `im.message.list`
2. Неверный `message_id` или `task_id`
3. У задачи нет `chatId` (старые задачи)
4. Сообщение было удалено

**Решение:**
1. Проверьте права вебхука в настройках Bitrix24
2. Проверьте логи для определения конкретной ошибки
3. Убедитесь, что задача имеет `chatId` (новые задачи)

### Проблема: Метод работает, но возвращает пустой текст

**Возможные причины:**
1. Комментарий содержит только файлы без текста
2. Комментарий был удален или изменен
3. Неверный формат ответа API

**Решение:**
1. Проверьте комментарий в интерфейсе Bitrix24
2. Проверьте логи для просмотра полного ответа API
3. Убедитесь, что используете правильный `message_id` из вебхука

## История изменений

- **2025-01-XX**: Добавлена система с 12 пронумерованными методами получения текста комментариев
- **2025-01-XX**: Интеграция с `handle_task_comment_event()` для автоматического использования
- **2025-01-XX**: Добавлена подробная документация по методу `im.dialog.messages.get` (см. [IM_DIALOG_MESSAGES_GET_API.md](IM_DIALOG_MESSAGES_GET_API.md))

## Дополнительная документация

- [IM_DIALOG_MESSAGES_GET_API.md](IM_DIALOG_MESSAGES_GET_API.md) - Подробное описание метода `im.dialog.messages.get` и его параметров
