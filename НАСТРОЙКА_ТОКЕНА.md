# Настройка токена исходящего вебхука

## 🔑 Ваш токен

Ваш токен исходящего вебхука: **`ssmtodolnza09g2rnm1ucbyzumejtr39`**

## ✅ Что нужно сделать

### Шаг 1: Добавить токен в переменные окружения

**Для Railway:**
1. Откройте проект в Railway
2. Перейдите в раздел **Variables**
3. Добавьте переменную:
   - **Name:** `BITRIX24_OUTGOING_WEBHOOK_TOKEN`
   - **Value:** `ssmtodolnza09g2rnm1ucbyzumejtr39`

**Для локальной разработки:**
Добавьте в файл `.env`:
```env
BITRIX24_OUTGOING_WEBHOOK_TOKEN=ssmtodolnza09g2rnm1ucbyzumejtr39
```

### Шаг 2: Перезапустить приложение

После добавления переменной перезапустите приложение, чтобы изменения вступили в силу.

## 🔒 Зачем нужен токен?

Токен используется для **безопасности** - он проверяет, что запросы действительно приходят от Bitrix24, а не от злоумышленников.

**Без токена:** Любой может отправлять запросы на ваш endpoint `/api/bitrix/webhook`  
**С токеном:** Только Bitrix24 с правильным токеном может отправлять запросы

## 📋 Полный список переменных для исходящего вебхука

Для работы исходящего вебхука нужны две переменные:

1. **`WEBHOOK_URL`** - URL вашего приложения
   - Пример: `https://your-app-name.railway.app`

2. **`BITRIX24_OUTGOING_WEBHOOK_TOKEN`** - токен исходящего вебхука
   - Ваш токен: `ssmtodolnza09g2rnm1ucbyzumejtr39`

## 🧪 Проверка работы

После настройки токена:

1. Обновите профиль пользователя в Bitrix24 (измените поле с Telegram ID)
2. Проверьте логи приложения
3. Должно появиться:
   ```
   ✅ Токен исходящего вебхука проверен успешно
   ✅ Синхронизировано из Bitrix24 (исходящий вебхук): Telegram ID X → Bitrix24 User ID Y
   ```

Если токен неверный, в логах появится:
```
⚠️ Неверный токен исходящего вебхука. Получен: ...
```

## ❓ Где Bitrix24 отправляет токен?

Bitrix24 может отправлять токен в разных местах:
- В заголовке `X-Bitrix-Token`
- В параметре URL `?token=xxx`
- В теле запроса в поле `token`

Обработчик проверяет все эти варианты автоматически.

## 📚 Дополнительная информация

См. также:
- [`ИНСТРУКЦИЯ_ВЕБХУК.md`](ИНСТРУКЦИЯ_ВЕБХУК.md) - полная инструкция по настройке
- [`BITRIX_OUTGOING_WEBHOOK.md`](BITRIX_OUTGOING_WEBHOOK.md) - подробная документация
