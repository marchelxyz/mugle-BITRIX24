# Ответ: Проблема с токеном исходящего вебхука Bitrix24

## 📋 Какие переменные используются?

### Обязательная переменная:

**`BITRIX24_OUTGOING_WEBHOOK_TOKEN`**
- **Что это:** Токен исходящего вебхука, который вы получили при создании вебхука в Bitrix24
- **Где установить:** В переменных окружения на сервере (Railway → Variables)
- **Пример значения:** `ssmtodolnza09g2rnm1ucbyzumejtr39`
- **Статус:** ❌ Скорее всего не установлена (иначе токен был бы найден)

### Опциональные переменные:

**`WEBHOOK_URL`**
- **Что это:** URL вашего приложения
- **Пример:** `https://web-production-de105.up.railway.app`
- **Статус:** ✅ Уже используется (видно из логов)

## 🔍 Что нужно вставить в переменные?

### 1. Установите переменную `BITRIX24_OUTGOING_WEBHOOK_TOKEN`

**Где взять токен:**
1. Войдите в Bitrix24
2. Перейдите в **Настройки** → **Приложения** → **Исходящий вебхук**
3. Найдите ваш вебхук (который отправляет события на `/api/bitrix/webhook`)
4. Скопируйте токен из настроек вебхука

**Как установить в Railway:**
1. Откройте проект в Railway
2. Перейдите в раздел **Variables**
3. Нажмите **"New Variable"**
4. **Name:** `BITRIX24_OUTGOING_WEBHOOK_TOKEN`
5. **Value:** вставьте токен из Bitrix24 (например, `ssmtodolnza09g2rnm1ucbyzumejtr39`)
6. Сохраните и перезапустите приложение

## ❌ Проблема

В логах видно:
```
⚠️ Неверный токен исходящего вебхука. Получен: None...
```

Это означает, что:
1. **Либо** переменная `BITRIX24_OUTGOING_WEBHOOK_TOKEN` не установлена
2. **Либо** Bitrix24 не отправляет токен в запросе (не настроен в Bitrix24)

## ✅ Решение

### Шаг 1: Проверьте переменную окружения

Убедитесь, что переменная `BITRIX24_OUTGOING_WEBHOOK_TOKEN` установлена в Railway.

### Шаг 2: Проверьте настройки вебхука в Bitrix24

1. Войдите в Bitrix24
2. Перейдите в **Настройки** → **Приложения** → **Исходящий вебхук**
3. Найдите вебхук с URL: `https://web-production-de105.up.railway.app/api/bitrix/webhook`
4. Убедитесь, что токен указан в настройках вебхука
5. Если токена нет - создайте новый вебхук и скопируйте токен

### Шаг 3: Перезапустите приложение

После установки переменной перезапустите приложение в Railway.

### Шаг 4: Проверьте логи

После перезапуска в логах должно появиться:
```
✅ Токен исходящего вебхука проверен успешно
```

Вместо:
```
⚠️ Неверный токен исходящего вебхука. Получен: None...
```

## 🔧 Дополнительная диагностика

Я добавил подробное логирование в код. Теперь в логах будет видно:
- Установлена ли переменная `BITRIX24_OUTGOING_WEBHOOK_TOKEN`
- Откуда пытается извлечь токен (заголовки, URL, тело запроса)
- Все заголовки запроса от Bitrix24

Это поможет точно определить, где проблема.

## 📚 Дополнительная информация

- [`ПРОБЛЕМА_ТОКЕНА.md`](ПРОБЛЕМА_ТОКЕНА.md) - подробное описание проблемы
- [`НАСТРОЙКА_ТОКЕНА.md`](НАСТРОЙКА_ТОКЕНА.md) - инструкция по настройке токена
- [`BITRIX_OUTGOING_WEBHOOK.md`](BITRIX_OUTGOING_WEBHOOK.md) - документация по вебхуку
